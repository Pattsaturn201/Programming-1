# include<stdio.h>

const num=0;;
int i;
int divisorcount=0;


int main(void)
{
  printf("Enter a integer number to verify if it is prime"); //A little introduction
  scanf("%d",num);
  
  if (num<=1) //we dertermine prime number definition
  {
    printf("the number is not prime");
  }
  else
  {
    
  } 
  return 0;
}